{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "baseUrl": ".", // this must be specified if "paths" is specified.
    "paths": {
      "@env": ["node_modules/@types/react-native-dotenv"] // this mapping is relative to "baseUrl"
    }
  }
}
